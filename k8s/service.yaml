apiVersion: v1
kind: Service
metadata:
  name: litellm
  namespace: lllm
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "lllm"
spec:
  selector:
    app: litellm
  ports:
    - port: 4000
      targetPort: 4000
  type: ClusterIP
